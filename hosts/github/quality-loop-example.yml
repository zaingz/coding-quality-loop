name: Quality Loop

on:
  pull_request:
  workflow_dispatch:

jobs:
  quality-loop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: zaingz/coding-quality-loop@v3.0.0
        with:
          record: agent-record.json
          base: origin/${{ github.base_ref || 'main' }}
          red-green: "false"
