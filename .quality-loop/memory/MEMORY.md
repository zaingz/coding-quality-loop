# Project Memory (index)

6 lesson(s). Recall detail with: python3 scripts/quality_loop.py memory-recall.

- [convention/medium] docs/control-plane.md joined the doc-count lint's watched docs list in run_evals.py, and its add-on count is stated in the lintable '37 add-on cases' phrasing; The 35-vs-37 drift persisted because the lint never watched that file AND the old phrasing was invisible to its patterns; the count-drift failure class had recurred (116->121 historically), so per RETROSPECT doctrine the fix is a durable lint extension, toggle-tested failing-then-green.; evals/run_evals.py [REDACTED] docs list; lint fails with '35 add-on cases' != 37 when toggled
- [preference/medium] Prefer rung 'delete': The task is subtraction by mandate: remove dead readers-without-writers, unify duplicate implementations into the existing shared module, and fix docs to match code. Deletion and reuse of existing primitives are the whole change; no new code beyond import lines and pointer sentences.
- [gotcha/medium] Review note: blocking; codex-gpt5-fresh-reviewer; SessionStart dedup changes missing-git behavior (crash -> cwd fallback), read as violating the no-behavior-change constraint; Kept with rationale, not reverted: the contract authorizes deletions whose behavior delta is named and justified (it is, in the commit message and CHANGELOG), and the prior crash violated the hook shims' documented never-crash contract; the divergence was the drift the dedup existed to fix.
- [gotcha/medium] Review note: blocking; codex-gpt5-fresh-reviewer; quality-loop.config.json declared a $schema it does not satisfy and claimed check-config reports missing orchestration sections (stale since the v6.3.0 gate-config-only path); Fixed in 0b0dfad: description now states the gate-config-only behavior; the false $schema pointer dropped. Follow-up noted: an eval pinning schema<->check-config agreement.
- [gotcha/medium] Review note: minor; codex-gpt5-fresh-reviewer; brief --help still advertised 'last run' after the run-journal deletion; Fixed in 0b0dfad: help now reads (last record, risks, lessons, progress).
- [gotcha/medium] Review note: informational; codex-gpt5-fresh-reviewer; Read-only review sandbox limited independent re-runs (TemporaryDirectory/pyc//tmp writes, 16 Node fs tests EPERM); check-config + 17 Node tests + in-memory compile of all 18 py files passed there; Documented sandbox reality (hosts/codex/README.md); the full battery re-runs in CI, which is the anchor.
