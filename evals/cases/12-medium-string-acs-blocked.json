{
  "name": "medium-string-acceptance-criteria-blocked",
  "description": "At medium+ risk a bare string acceptance criterion is unprovable: the AC-coverage gate blocks it with an actionable message (convert to an object with a proving_command). Everything else about the record is compliant, so the only finding is the AC one.",
  "record": {
    "task_id": "t-12",
    "goal": "Add bounded exponential backoff to the HTTP client retry path",
    "task_class": "medium",
    "risk_tier": "medium",
    "acceptance_criteria": ["Retries use bounded exponential backoff; caller behavior unchanged"],
    "verification_plan": ["unit tests for the backoff schedule"],
    "minimality_decision": {"rung": "reuse", "reason": "extend the existing retry helper"},
    "plan": ["add backoff to client"],
    "commands_run": [{"cmd": "pytest tests/test_client.py", "class": "unit", "result": "pass", "evidence": "18 passed"}],
    "open_risks": [],
    "review_findings": ["fresh-context review: approved"],
    "repo_map": {
      "entry_points": ["client/http.py:retry"],
      "likely_files": ["client/http.py"],
      "callers_checked": ["svc/a.py:call"],
      "tests": ["tests/test_client.py"]
    },
    "implementer": "agent-a",
    "validation_contract": {"goal": "Bounded backoff", "acceptance_criteria": ["backoff is capped"], "evidence": ["pytest tests/test_client.py -> pass"]},
    "independent_review": {"reviewer": "agent-b", "verdict": "approve", "fresh_context": true, "patched": false, "findings": []},
    "security_review": null,
    "completion_record": {"goal": "Bounded backoff", "acceptance_criteria": ["backoff is capped"], "evidence": ["pytest -> 18 passed", "fresh-context review: approve"]},
    "security_sensitive": false,
    "status": "done"
  },
  "expect": {
    "risk_floor": "low",
    "gates_exit": 1,
    "findings_include": ["has no proving_command", "proving_command"]
  }
}
